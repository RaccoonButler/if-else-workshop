console.log("Hi! This is the number Line Exercise")
//Testing the script
let num1 = 50; 
  let num2 = 51; 
//Declares the numbe values
  let sum = num1 + num2;
//Adds them together as a sum
  if (sum < -1000) {
    console.log(sum + " is less than -1000");
  } else if (sum < 0) {
    console.log(sum + " is a negative number");
  } else if (sum === 0) {
    console.log(sum + " is equal to 0");
  } else if (sum > 0 && sum <= 100) {
    console.log(sum + " is larger than 0");
  } else if (sum > 100) {
    console.log(sum + " is greater than 100");
  }
  //Checks the sum to see if it's greater than or less than